<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>lo1see</title>
  <icon>https://www.gravatar.com/avatar/2d738efc6587f21b0f241473099e460e</icon>
  <subtitle>世界で一番菜的菜狗</subtitle>
  <link href="http://lo1see.github.io/atom.xml" rel="self"/>
  
  <link href="http://lo1see.github.io/"/>
  <updated>2023-06-29T15:45:43.983Z</updated>
  <id>http://lo1see.github.io/</id>
  
  <author>
    <name>lo1see</name>
    <email>2296161885@qq.com</email>
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>LitCTF  RE方向 WP</title>
    <link href="http://lo1see.github.io/2023/06/29/LitCTF%20RE%E6%96%B9%E5%90%91WP/"/>
    <id>http://lo1see.github.io/2023/06/29/LitCTF%20RE%E6%96%B9%E5%90%91WP/</id>
    <published>2023-06-29T14:30:36.066Z</published>
    <updated>2023-06-29T15:45:43.983Z</updated>
    
    <content type="html"><![CDATA[<p>主要从第四题开始有新知识点</p><span id="more"></span><h2 id="世界上最棒的程序员"><a href="#世界上最棒的程序员" class="headerlink" title="世界上最棒的程序员"></a>世界上最棒的程序员</h2><p>打开ida就能看到flag</p><h2 id="ez-XOR"><a href="#ez-XOR" class="headerlink" title="ez_XOR"></a>ez_XOR</h2><p>简单的异或，静态能看到异或的是9</p><h2 id="enbase64"><a href="#enbase64" class="headerlink" title="enbase64"></a>enbase64</h2><p>base64变表，变表代码CV过来就能用</p><h2 id="snake"><a href="#snake" class="headerlink" title="snake"></a>snake</h2><p>附件下载下来是个pyc文件，直接用uncomplyed6发现出不来，提示magic nuber错误。之后上网查一下各版本python的magic number试就试出来可以uncomplyed6反编译了</p><p><a href="https://imgse.com/i/p9cDMTI"><img src="https://s1.ax1x.com/2023/05/14/p9cDMTI.png" alt="p9cDMTI.png"></a></p><p>进入.py一眼就能看到flag</p><h2 id="For-Aiur"><a href="#For-Aiur" class="headerlink" title="For Aiur"></a>For Aiur</h2><p>又是一个python逆向，先用pyinstxtractor反编译出.pyc，发现uncomplyed6出不来，我就去了<a href="https://tool.lu/pyc/%E5%9C%A8%E7%BA%BF%E5%8F%8D%E7%BC%96%E8%AF%91%E5%B7%A5%E5%85%B7%E5%8F%8D%E7%BC%96%E8%AF%91%E5%87%BA%E6%9D%A5%E4%BA%86%E3%80%82">https://tool.lu/pyc/在线反编译工具反编译出来了。</a></p><p><a href="https://imgse.com/i/p9cDT1O"><img src="https://s1.ax1x.com/2023/05/14/p9cDT1O.png" alt="p9cDT1O.png"></a></p><p>观察代码发现有个ch库引入了check函数，再看眼函数整体可以发现</p><p><a href="https://imgse.com/i/p9crn3T"><img src="https://s1.ax1x.com/2023/05/14/p9crn3T.png" alt="p9crn3T.png"></a></p><p>只有这一个地方使用了check()，其他代码也都很正常，那么关键就在这个check函数了。</p><p>回到pyinstxtractor反编译出来的文件夹，搜索ch可以找到ch.pyc</p><p><a href="https://imgse.com/i/p9crlDJ"><img src="https://s1.ax1x.com/2023/05/14/p9crlDJ.png" alt="p9crlDJ.png"></a></p><p>使用uncomplyed6即可得到ch.py，打开直接就能看到加密很简单的flag</p><h2 id="程序和人有一个能跑就行了"><a href="#程序和人有一个能跑就行了" class="headerlink" title="程序和人有一个能跑就行了"></a>程序和人有一个能跑就行了</h2><p>拖进ida简单分析一下，可以发现一个很明显的RC4加密函数</p><p><a href="https://imgse.com/i/p9crLxU"><img src="https://s1.ax1x.com/2023/05/14/p9crLxU.png" alt="p9crLxU.png"></a></p><p>Buf1是RC4加密结果，写脚本嗦一下就可以轻松得到flag，不过是fake的</p><pre><code class="python">res_l = [0x8D, 0x6C, 0x85, 0x76, 0x32, 0x72, 0xB7, 0x40, 0x88, 0x7E, 0x95, 0xEE, 0xC5, 0xED, 0x2E, 0x71, 0x37, 0xF1, 0x4A, 0x99, 0x35, 0x18, 0xA7, 0xB0, 0x00, 0x96, 0xB7]res_b = bytes(res_l)seed = b&quot;litctf&quot;enc = ARC4.new(seed)inp = enc.decrypt(res_b)print(inp)</code></pre><p>然后接着分析代码。运行过几次之后发现输入错误只会显示wrong？输入正确只会显示right？，与ida的代码逻辑不相符</p><p><a href="https://imgse.com/i/p9cyM79"><img src="https://s1.ax1x.com/2023/05/14/p9cyM79.png" alt="p9cyM79.png"></a></p><p>这里不是if else，而是只有if，那么逻辑上讲right是一定会输出的，但事实没有。那么if分支中就有些异样了。换成汇编进入动态分析</p><p><img src="https://s1.imagehub.cc/images/2023/05/14/QQ20230514140325.png" alt="QQ20230514140325.png"></p><p>发现走过了sub_475190却依旧在执行一些ida没有反编译出来的代码。继续往下执行可以看到</p><p><img src="https://s1.imagehub.cc/images/2023/05/14/QQ20230514140446.png" alt="QQ20230514140446.png"></p><p>把这段16进制放入脚本执行RC4解密，就可以得到flag了</p><pre><code class="python">from Crypto.Cipher import ARC4res_l = [0x8D, 0x6C, 0x85, 0x76, 0x32, 0x72, 0xB7, 0x43, 0x85, 0x7B, 0x85, 0xDE, 0xC1, 0xFB, 0x2E, 0x64, 0x07, 0xC8, 0x5F, 0x9A, 0x35, 0x18, 0xAD, 0xB5, 0x15, 0x92, 0xBE, 0x1B, 0x88]res_b = bytes(res_l)seed = b&quot;litctf&quot;enc = ARC4.new(seed)inp = enc.decrypt(res_b)print(inp)</code></pre><p>问了下可能是C++异常处理的知识点</p><h2 id="debase64"><a href="#debase64" class="headerlink" title="debase64"></a>debase64</h2><p>拖进ida先静态分析，能看到输入20个字符到v15，在sub_401520函数将v15转换到了v4，再拿v4和v6-v14的每个字节进行比较。根据题目和进入sub_401520也可以基本确定sub_401520是个解base64的函数。</p><p><img src="https://s1.imagehub.cc/images/2023/05/14/QQ20230514140834.png" alt="QQ20230514140834.png"></p><p>之后进入动态调试，步过sub_401520的时候发现这不是普通的base64。动态分析后能发现，原理是把base64解密顺序反过来了，用第四个字符左移两位，第三个字符右移四位这样。故只需写个加密脚本，手动把顺序倒过来就好。因为flag后有3个“&#x3D;”，最后一位根据hint爆破即可。</p><pre><code>import base64list = [0x46, 0xED, 0x18, 0x96, 0x56, 0x9E, 0xD2, 0x72, 0xB2, 0xB3, 0x80, 0x70, 0xFF]list_b = bytes(list)print(base64.b64encode(list_b))</code></pre><p>Ru0Yllae0nKys4Bw&#x2F;w&#x3D;&#x3D;   –&gt;&gt;   Y0uReallyKn0wB4s&#x2F;w&#x3D;&#x3D;   –&gt;&gt;   Y0uReallyKn0wB4s?&#x3D;&#x3D;&#x3D;</p><pre><code>import hashlibkey = &quot;ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/&quot;for i in key:    str1 = &quot;Y0uReallyKn0wB4s&quot; + i + &quot;===&quot;    if hashlib.md5(str1.encode(&#39;utf-8&#39;)).hexdigest() == &quot;5a3ebb487ad0046e52db00570339aace&quot;:        print(str1)        break</code></pre><p>得到Y0uReallyKn0wB4s3&#x3D;&#x3D;&#x3D;</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;主要从第四题开始有新知识点&lt;/p&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>PELoader</title>
    <link href="http://lo1see.github.io/2023/03/18/MyLoader/"/>
    <id>http://lo1see.github.io/2023/03/18/MyLoader/</id>
    <published>2023-03-17T16:00:00.000Z</published>
    <updated>2023-03-17T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="PELoader？"><a href="#PELoader？" class="headerlink" title="PELoader？"></a>PELoader？</h2><p>首先要清楚PELoader是干什么的。</p><p>PELoader就相当于一个PE文件加载器，通过运行PELoader可以打开一个PE程序，该PE程序并不是系统为你打开的。运行起来的效果一般和双击是一样的。个人认为也是可以通过PEloader修改文件在内存中的数据的。</p><p>通过写PEloader可以让我们更清楚的了解PE文件的结构，以及运行原理，这对于理解计算机底层是十分有帮助的。</p><h2 id="IDE选择"><a href="#IDE选择" class="headerlink" title="IDE选择"></a>IDE选择</h2><p>推荐使用Visual Studio</p><p>师傅在辅导我写PEloader时，指导我使用了Visual Studio 2022。VS的调试功能十分强大，可以在逐步调试时查看内存，将C代码反汇编等等。在代码不能顺利运行时可以更好的进行调试。</p><p>VS的安全检查比较严格，自己的代码运行时需要关闭安全检查。还有完成后要注意自己要运行的程序是32位还是64位的程序，以及VS本身是x86运行还是x64运行。</p><h2 id="main-函数"><a href="#main-函数" class="headerlink" title="main()函数"></a>main()函数</h2><p>因为代码量不小，所以我选择了分为main函数和头文件，再调用头文件中的函数。</p><p>在main函数中</p><h3 id="1-文件路径"><a href="#1-文件路径" class="headerlink" title="1.文件路径"></a>1.文件路径</h3><p>我们需要获得文件的地址，也就是文件路径字符串。</p><h3 id="2-寻找文件"><a href="#2-寻找文件" class="headerlink" title="2.寻找文件"></a>2.寻找文件</h3><p>根据文件路径，通过<code>fopen()</code>函数找到文件，并给予权限。</p><pre><code>if ((fp = fopen(file, &quot;rb&quot;)) == NULL)&#123;    printf(&quot;Open file fail&quot;);    exit(1);&#125;</code></pre><h3 id="3-分配”磁盘”"><a href="#3-分配”磁盘”" class="headerlink" title="3.分配”磁盘”"></a>3.分配”磁盘”</h3><p>计算出文件大小，并分配出一块等大小的内存区域(我声明为<code>pBuf</code>)</p><pre><code>fseek(fp, 0, SEEK_END);last = ftell(fp);fsize = last;</code></pre><h3 id="4-复制"><a href="#4-复制" class="headerlink" title="4.复制"></a>4.复制</h3><p>将文件的二进制信息全部复制到这块内存区域中</p><pre><code>fseek(fp, 0, SEEK_SET);pBuf = (PBYTE)malloc(fsize);fread(pBuf, fsize, 1, fp);</code></pre><h2 id="Check一下PE"><a href="#Check一下PE" class="headerlink" title="Check一下PE"></a>Check一下PE</h2><p>在将文件读入<code>malloc()</code>给予的内存空间中后，我们拥有了对文件进行操作的权限。在此我将<code>pBuf</code>视为在磁盘中，稍后再将“磁盘”中的文件信息传入内存。</p><p>对<code>pBuf</code>进行一个检查，check以下该文件是不是PE文件，若不是，则退出程序。</p><p>判断PE文件方法即为判断DOS头中的<code>e_magic</code>和NT头中的<code>Signature</code>签名。</p><pre><code>if (pDOSheader-&gt;e_magic == IMAGE_DOS_SIGNATURE) &#123;    if (pNTheader-&gt;Signature == IMAGE_NT_SIGNATURE) &#123;        return true;        &#125;    &#125;</code></pre><h2 id="LoadPE"><a href="#LoadPE" class="headerlink" title="LoadPE"></a>LoadPE</h2><p>这里开始进行最主要的加载过程</p><h3 id="1-定位"><a href="#1-定位" class="headerlink" title="1.定位"></a>1.定位</h3><p>利用传入的参数<code>pBuf</code>，得到DOS头，NT头，节区头并赋值</p><pre><code>PIMAGE_DOS_HEADER pDOSheader = (PIMAGE_DOS_HEADER)pBuf;//赋值DOS头PIMAGE_NT_HEADERS32 pNTheader = (PIMAGE_NT_HEADERS32)(pBuf + pDOSheader-&gt;e_lfanew);//赋值NT头PIMAGE_SECTION_HEADER pSecheader = (PIMAGE_SECTION_HEADER)((PBYTE)(pNTheader)+ 0x18 + (pNTheader-&gt;FileHeader.SizeOfOptionalHeader));//赋值节区头</code></pre><h3 id="2-分配”内存”"><a href="#2-分配”内存”" class="headerlink" title="2.分配”内存”"></a>2.分配”内存”</h3><p>通过<code>VirtualAlloc()</code>函数分配出一片和PE文件同样大的内存区域~，这个大小在可选头的<code>SizeOfImage</code>有保存。我们将这片区域<code>pAlloc</code>视为“内存”中。</p><pre><code>PBYTE pAlloc = (PBYTE)VirtualAlloc(NULL, pNTheader-&gt;OptionalHeader.SizeOfImage, MEM_COMMIT | MEM_RESERVE, PAGE_EXECUTE_READWRITE);</code></pre><p>然后对这片区域利用<code>memset()</code>进行初始化。之后就可将在“磁盘”中的<code>pBuf</code>的文件头加载到“内存”区域的pAlloc。<code>pAlloc</code>的地址就是程序运行时的加载地址。</p><h3 id="3-加载节区"><a href="#3-加载节区" class="headerlink" title="3.加载节区"></a>3.加载节区</h3><p>通过一二步之后，准备工作就完成了。接下来要开始循环加载节区信息。</p><p>通过学习PE文件的知识，我们知道了磁盘文件或内存的节区大小必定为<code>FileAlignment</code>或<code>SectionAlignment</code>值的整数倍，若不为整数倍，计算机会自动补00对齐。</p><p>通过010 editor观察文件易发现节区头的<code>VirtualAddress</code>和<code>PointToRawData</code>是已经对齐好的，我们不需要写偏移函数来手动对齐。所以只需要</p><pre><code>memmove(pAlloc + pSecheader-&gt;VirtualAddress, pBuf + pSecheader-&gt;PointerToRawData, pSecheader-&gt;Misc.VirtualSize);</code></pre><p>就可将节区加载进内存。但后面要补的0该怎么办呢？其实早在初始化<code>pAlloc</code>时就已经将不加载数据的区域补了0，并不需要担心。</p><p>加载完一个节区之后不要忘记进入下一个节区继续加载</p><pre><code>pSecheader = (PIMAGE_SECTION_HEADER)((PBYTE)pSecheader + (BYTE)(sizeof(IMAGE_SECTION_HEADER)));</code></pre><h3 id="4-加载重定位表"><a href="#4-加载重定位表" class="headerlink" title="4.加载重定位表"></a>4.加载重定位表</h3><p>加载完节区之后，我们还有重定位表和导入表没有处理。先处理重定位表。</p><p>值得注意的是有的PE文件并<strong>不存在重定位表</strong>，若重定位表地址为00000000，则我们应直接跳过重定位表的处理；有的PE文件<strong>重定位表也不止一个</strong>，不过可选头中给的重定位表的大小是所有重定位表的总和，利用这个大小即可知道是否处理完了重定位表。</p><p>先定位找到重定位表，并确定重定位表的大小</p><pre><code>PIMAGE_BASE_RELOCATION pBaseReloc = (PIMAGE_BASE_RELOCATION)(pNTheader-&gt;OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_BASERELOC].VirtualAddress + pAlloc);int SizeOfBaseReloc = pNTheader-&gt;OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_BASERELOC].Size;</code></pre><p>这样就算有多个重定位表，我们也能找到各个重定位表的地址和大小了。</p><pre><code>pBaseReloc-&gt;VirtualAddress;pBaseReloc-&gt;SizeOfBlock;</code></pre><p>通过这两个数据即可得到块的数量和每个块的数据。</p><p>通过对重定位表的学习，我们已清楚重定位原理。先利用位操作得到每个块的前四位，判断每个块的类型，一般为3；并同时得到后12位的偏移地址</p><pre><code>WORD type = TypeOffset[i] &gt;&gt; 12;WORD offset = TypeOffset[i] &amp; 0x0FFF;</code></pre><p>偏移地址加上<code>VirtualAddress</code>可得出一个地址，该地址存储着程序的硬编码地址，应读取该值之后减去<code>ImageBase</code>值，再加上实际加载地址pAlloc，得到实际重定位后的硬编码地址。</p><pre><code>*((DWORD*)(offset + (pBaseReloc-&gt;VirtualAddress) + pAlloc)) - pNTheader-&gt;OptionalHeader.ImageBase + (DWORD)pAlloc;</code></pre><p>我们将这个值覆盖存储到原先的硬编码地址，重定位就完成了。</p><p><em>若还有多个重定位表，记得在加载完成后给<code>pBaseReloc</code>加上一个<code>SizeOfBlock</code></em></p><pre><code>pBaseReloc = (PIMAGE_BASE_RELOCATION)((PBYTE)pBaseReloc + pBaseReloc-&gt;SizeOfBlock);</code></pre><p>接着回去加载下一个重定位表</p><h3 id="5-加载导入表"><a href="#5-加载导入表" class="headerlink" title="5.加载导入表"></a>5.加载导入表</h3><p>重定位表处理完成后，就该处理导入表了。</p><p>导入表拥有多个dll文件，dll文件中也有很多函数，要循环加载。</p><p>先声明得到导入表的位置</p><pre><code>PIMAGE_IMPORT_DESCRIPTOR pImport = (PIMAGE_IMPORT_DESCRIPTOR)(pNTheader-&gt;OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_IMPORT].VirtualAddress + pAlloc);</code></pre><p><code>pImport-&gt;Name</code>就是dll文件的文件名字符串的地址，再加上加载地址<code>pAlloc</code>找到字符串，利用win32的LoadLibrary()函数加载dll文件进入内存。</p><p>加载完dll之后就可以计算IAT和INT的地址，开始加载dll中的函数了。</p><p><strong>IAT和INT下都有一个联合体(union)，为u1</strong></p><pre><code>typedef struct _IMAGE_THUNK_DATA32 &#123;    union &#123;        DWORD ForwarderString;      // PBYTE         DWORD Function;             // PDWORD        DWORD Ordinal;        DWORD AddressOfData;        // PIMAGE_IMPORT_BY_NAME    &#125; u1;&#125; IMAGE_THUNK_DATA32;typedef IMAGE_THUNK_DATA32 * PIMAGE_THUNK_DATA32;</code></pre><p>拥有四个元素，这四个元素公用了同一个<code>DWORD</code>，即为同一个值。但该值可拥有多种作用，便有了多种写法。</p><p>在导入函数时，先判断一下函数是使用哪种方法导入的。</p><p>若是用序号导入函数，则INT中存储的地址最高位即为1。故进行判断</p><pre><code>if (pINT-&gt;u1.AddressOfData &amp; IMAGE_ORDINAL_FLAG32)</code></pre><p>若为真，则函数使用序号导入，而不使用字符串来查找函数</p><pre><code>pIAT-&gt;u1.AddressOfData = (DWORD)(GetProcAddress(hProcess, (LPCSTR)(pINT-&gt;u1.Ordinal)));</code></pre><p>若为假，就可能需要使用字符串查找函数地址</p><pre><code>pIAT-&gt;u1.AddressOfData = (DWORD)(GetProcAddress(hProcess, pFucname-&gt;Name));</code></pre><p>函数导入完毕，别忘了</p><pre><code>pINT++;pIAT++;</code></pre><p>导入完一个dll之后也要</p><pre><code>pImport++;</code></pre><p><em>这里的+1是加了<code>IMAGE_IMPORT_DESCRIPTOR</code>一个结构体的大小，而不是一个字节，所以可以跳到下一个dll的导入表。</em></p><h2 id="进入main-函数"><a href="#进入main-函数" class="headerlink" title="进入main()函数"></a>进入main()函数</h2><p>至此，已load完成。接下来就是利用函数指针，指向<code>EntryPoint</code>，即可调用主函数，运行程序。</p><h2 id="Congratulation！"><a href="#Congratulation！" class="headerlink" title="Congratulation！"></a>Congratulation！</h2><p>顺利的话程序这样就已经运行起来了，恭喜。</p><h1 id="致谢"><a href="#致谢" class="headerlink" title="致谢"></a><strong>致谢</strong></h1><p>非常感谢Jev0n，s0rry，l0v3ming师傅的帮助。由于自身过于愚笨，全篇PEloader写的bug百出，内容思路也仿照（抄）Jev0n师傅的loader，忙了师傅们一天才帮忙调完可以运行。完成度也很低，只能做到最低要求的运行起一个PE文件。</p><p>不过最终运行起来的时候还是挺开心的，从中学到了不少东西，复习了PE文件结构的相关知识，并认识的更深了。师傅说学好windows逆向，熟悉PE结构是十分重要的。</p><p>我希望我之后能誊出时间写一份完成度更高的PEloader。</p><p>谢谢师傅们的指导（鞠躬）</p><h1 id="MyPEloader链接"><a href="#MyPEloader链接" class="headerlink" title="MyPEloader链接"></a>MyPEloader链接</h1><p><a href="https://github.com/lo1see/SomeGarbage">https://github.com/lo1see/SomeGarbage</a></p>]]></content>
    
    
    <summary type="html">随便写的PELoader草记</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>Hello World</title>
    <link href="http://lo1see.github.io/2023/03/17/hello-world/"/>
    <id>http://lo1see.github.io/2023/03/17/hello-world/</id>
    <published>2023-03-17T15:32:15.717Z</published>
    <updated>2023-06-29T15:46:49.285Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. </p><span id="more"></span><p>Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre><code class="bash">$ hexo new &quot;My New Post&quot;</code></pre><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre><code class="bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre><code class="bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre><code class="bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Welcome to &lt;a href=&quot;https://hexo.io/&quot;&gt;Hexo&lt;/a&gt;! This is your very first post. &lt;/p&gt;</summary>
    
    
    
    
  </entry>
  
</feed>
